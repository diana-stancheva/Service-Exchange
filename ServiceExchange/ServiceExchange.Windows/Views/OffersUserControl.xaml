<UserControl
    x:Class="ServiceExchange.Views.OffersUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ServiceExchange.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <GridView x:Name="OffersControl" ItemsSource="{Binding Skills}" SelectionMode="None">
        <GridView.ItemsPanel>
            <ItemsPanelTemplate>
                <ItemsWrapGrid Margin="3" ItemWidth="240" Orientation="Horizontal" MaximumRowsOrColumns="3"/>
            </ItemsPanelTemplate>
        </GridView.ItemsPanel>

        <GridView.ItemTemplate>
            <DataTemplate>
                <GridViewItem Background="#FF00ABA9" Margin="0" Width="240" Padding="10">
                    <StackPanel HorizontalAlignment="Left">
                        <TextBlock TextAlignment="Left" TextWrapping="Wrap" Text="{Binding Name}" FontSize="20" Foreground="#E9EEEE" FontWeight="Bold"/>
                        <TextBlock TextAlignment="Left" TextWrapping="Wrap" Text="{Binding User}" FontSize="20" Foreground="#E9EEEE" FontWeight="Bold"/>
                    </StackPanel>
                </GridViewItem>
            </DataTemplate>
        </GridView.ItemTemplate>
    </GridView>
</UserControl>
