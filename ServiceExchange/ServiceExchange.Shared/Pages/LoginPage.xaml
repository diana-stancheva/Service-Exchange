<Page
    x:Class="ServiceExchange.Pages.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ServiceExchange.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >
    <ScrollViewer>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <StackPanel Style="{StaticResource SECenteredContainerStackPanel}">
                <TextBlock Text="Welcome to" Style="{StaticResource SEWelcomeTextBlockAppTitle}" />
                <TextBlock Text="ServiceExchange" Style="{StaticResource SELoginTextBlockAppTitle}" />

                <TextBox Name="username" PlaceholderText="Username" Style="{StaticResource SETextBox}"/>
                <PasswordBox Name="password" PlaceholderText="Password" Style="{StaticResource SEPasswordBox}"  />
                <Button Content="Login" Style="{StaticResource SELoginButton}" Click="Button_Click" />

                <TextBlock Text="New to ServiceExchange?" Margin="0 100 0 0" Style="{StaticResource BottomTextBlockDarkStyleCentered}" />
                <TextBlock Text="SignUp Now!" Style="{StaticResource BottomTextBlockDarkStyleCentered}" FontWeight="Bold" Tapped="TextBlock_Tapped"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
